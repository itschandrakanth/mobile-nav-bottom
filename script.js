/*for test no-js vs js just comment out this bit - codepen doesn't work without js enabled, so just have fun this way*/
document.addEventListener("DOMContentLoaded",     function() {
  //document.body.classList.add("js");
});